# Convex deployment URL (from Convex dashboard)
NEXT_PUBLIC_CONVEX_URL=

# Convex CLI deployment slug (optional but recommended for local tooling)
CONVEX_DEPLOYMENT=

# Convex Auth / Auth.js settings
AUTH_SECRET=
AUTH_DISCORD_ID=
AUTH_DISCORD_SECRET=

# Discord OAuth Redirect URI (configure in Discord Developer Portal)
# https://<your-convex-site-domain>/api/auth/callback/discord
